@startuml
start

:用户查询 + 初始检索器召回文档;
:送入 **黑盒 LLM** 进行自适应标注;
:LLM 输出：相关性标签 + 证据片段 + 分数;
:构建 Pairwise 排序损失;
:仅微调检索器;
:自引导数据筛选（选取高置信样本）;

repeat
    :检索器重新召回文档;
    :黑盒 LLM 自适应标注;
    :微调配齐检索器;
repeat while (达到停止条件?) is (否)

:是;
:得到对齐黑盒LLM的检索器;
:上线推理：查询 → 检索 → LLM生成答案;

end
@enduml