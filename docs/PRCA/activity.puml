@startuml
start

:用户查询;
:检索器召回初始候选文档;
:文档输入黑盒LLM生成答案;
:计算奖励信号（相关性 + 无幻觉）;
:将查询、文档、奖励输入 PRCA 适配器;
:适配器进行强化学习训练;

repeat :迭代训练;
    :更新适配器参数;
    :重新检索 -> 生成答案 -> 计算奖励;
repeat while (模型未收敛?) is (是);

:训练完成，得到对齐后的 PRCA 适配器;
:上线推理：查询 -> 检索 -> PRCA 适配 -> LLM 生成最终答案;

end
@enduml