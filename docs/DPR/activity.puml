@startuml
start
:加载预训练LLM（未SFT）;
:构建预对齐训练数据（外部对齐输出的高偏好文档）;
:构造prompt：问题 + 偏好文档 + 约束;
:开始内部预对齐训练;

repeat
    :前向传播：依据文档生成回答;
    :计算语言建模损失(LM Loss);
    :反向传播更新LLM参数;
    :验证：是否优先使用文档知识;
repeat while (未达到收敛?) is (否)

:内部预对齐完成;
:进入SFT指令微调阶段;
:得到对齐检索知识的最终LLM;

end
@enduml